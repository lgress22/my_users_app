<!DOCTYPE html>
<html>
    <head>
        <title>Users index page</title>
        <style>
        h1 {
            color: #333;
            text-align: center;
        }

        table {
            border-collapse: collapse;
            width: 100%
        }

        th, td {
            padding: 8px;
            text-align: center;
            border-bottom: 1px solid #ddd;
        }
        </style>
    </head>
    <body>
        <h1>Users</h1>
        <table>
            <tr>
                <th>FirstName</th>
                <th>LastName</th>
                <th>Age</th>
                <th>Email</th>
            </tr>
            <tr>
            <% @users.each do |user| %>
                <td><%= user[:firstname] %></td>
                <td><%= user[:lastname] %></td>
                <td><%= user[:age] %></td>
                <td><%= user[:email] %></td>
            </tr>
            <% end %>
        </table>
    </body>
</html>